# ProTrail v0.1.2

## Scope

Source-only documentation patch. Adds the complete Croatian translation set for
the seven maintained documentation surfaces and updates release-facing metadata
to the `v0.1.2` identity.

The published `v0.1.1` tag remains immutable. This patch does not replace or
retag that release.

## Release status

No official Windows executable, installer, portable archive, package-manager
binary, or signed binary is included. The final product-icon gate remains open:

`FINAL_BINARY_RELEASE_BLOCKED: USER_PRODUCT_ICON_PENDING`

## Verification

The Croatian `hr` package is checked for exact file scope, Markdown heading and
fence parity, preserved commands/paths/inline-code tokens, resolved links, and
33-locale switchers. No source-digest marker is appended to those documents.
