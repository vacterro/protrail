# User request

priority: P1
verify: the requested change is present and demonstrated against the user own description of it

## Request

ProTrail

MISSION

Take the current converged ProTrail repository from its present DONE state through final official Windows binary release.

This is an end-to-end release corridor, not a single repair wave.

Continue autonomously across milestones without asking "continue?" after each milestone.

The target is a genuinely publishable Windows x64 release with:
- canonical clean source state;
- final accepted product branding;
- correct Windows executable resources;
- reproducible Release build;
- deployed Qt runtime package;
- binary smoke verification;
- release metadata consistency;
- cryptographic checksums;
- immutable release version/tag;
- published release assets when repository credentials and remote publication capability are available;
- canonical SAIPEN closure and cold-recovery evidence.

QUALITY > TIME.

Do not stop merely because one milestone completes.
Continue until:
A. the official release is published and independently verified; or
B. no eligible autonomous work remains because of a genuine external capability/user dependency.

CURRENT MACHINE TRUTH

Snapshot:
- project: ProTrail
- VERSION: 0.1.0
- SAIPEN phase: DONE
- active task: none
- BOARD DOING: empty
- BOARD TODO: empty
- convergence target: crew
- last recorded SAIPEN event in supplied snapshot: E-648
- source audit T-57: DONE
- implementation repair T-59: DONE
- SubSaipen review T-60/T-61/T-62: DONE
- forced HUNT: CLEAN
- final forced HUNT: CLEAN
- canonical validation gate: PASS
- Release CTest: 22/22 PASS in latest recorded convergence evidence
- previous clean Release verification: /W4 /WX, zero errors/warnings
- audit/2.md findings: terminal
- SRC-017: consumed/closed

The previous protocol-level T-59 SHIP problem is no longer the current work target.
Do not reopen it unless the live repository proves a new active blocker.

CURRENT RELEASE STATUS

The repository explicitly describes v0.1.0 as SOURCE-ONLY.

Current binary-release blocker:

FINAL_BINARY_RELEASE_BLOCKED: USER_PRODUCT_ICON_PENDING

RELEASE_BLOCKERS.md explicitly forbids official distribution of:
- EXE;
- installer;
- portable binary package;
- signed executable;
- branded Windows binary release;

until the final ProTrail product icon is supplied and accepted.

The generated QPainter tray cursor icon is only a development fallback.

The final icon integration contract explicitly includes:
- Windows executable resource;
- taskbar/window icon;
- tray icon policy;
- multi-resolution ICO sizes;
- enabled/disabled tray-state recognizability;
- packaging metadata;
- final binary-release gate.

Do not bypass this contract.

IMPORTANT GIT TRUTH FROM THE SUPPLIED AUDAPACK SNAPSHOT

The archive does NOT contain .git itself.

AUDAPACK reports:
- git_head:
  30fa80267a86068a10f8bc7529ca81a4a5842e60
- git_dirty: true
- tracked: 936
- untracked: 283
- tracked_deleted: 11

Tracked deletions include intentional-looking architectural removals such as:
- src/ui/main_window.cpp
- src/ui/main_window.h
- tests/test_main_window.cpp

but they MUST be reconciled against the live Git repository before release.

Never publish a release directly from this archived snapshot.
The live repository is authoritative.

There is also historical evidence that tag v0.1.0 already exists.

Never force-move, delete/recreate, or silently replace an existing release tag.

RELEASE AUTHORIZATION

This handoff explicitly requests completion of the release lifecycle, including Git commit/tag/push and remote release publication once all release gates are green.

Do not request routine approval between milestones.

This authorization does NOT permit:
- bypassing a failed validation gate;
- overwriting an existing tag;
- rewriting published history;
- inventing signing credentials;
- inventing final branding;
- publishing from a dirty or ambiguous source tree;
- publishing an artifact whose provenance cannot be established.

======================================================================
MILESTONE A — COLD RECOVERY AND LIVE REPOSITORY RECONCILIATION
======================================================================

Start from the live ProTrail repository.

Read canonical state:
- .saipen/STATE.md
- .saipen/BOARD.md
- .saipen/LOG.md tail
- .saipen/MANIFEST.json
- VERSION
- RELEASE_BLOCKERS.md
- RELEASE_NOTES_v0.1.0.md
- CHANGELOG.md
- README.md
- CONTRIBUTING.md
- SECURITY.md
- CMakeLists.txt
- .github/workflows/ci.yml
- deploy.bat
- smoke_harness.ps1

Inspect real Git state:
- branch
- HEAD
- upstream
- status
- staged state
- unstaged state
- untracked files
- deleted tracked files
- tags
- remote tags
- current origin
- divergence from upstream
- commits since existing release tag

Do not assume the AUDAPACK inventory is still current.

Classify every dirty-tree group as one of:
1. intended product change;
2. intended protocol/state change;
3. generated/runtime artifact;
4. obsolete file intentionally removed;
5. unexplained residue.

No unexplained residue may survive into the release commit.

Acceptance:
- exact release baseline is known;
- no accidental staging exists;
- current branch/upstream relationship is known;
- existing v0.1.0 tag/release status is known;
- dirty-tree contents are classified;
- no history is rewritten.

======================================================================
MILESTONE B — RELEASE VERSION IDENTITY
======================================================================

Determine the correct immutable release version from live Git truth.

RULE:

If v0.1.0 already exists as a Git tag or published release and the current release tree differs from that immutable revision:

DO NOT reuse v0.1.0.

Default to the next valid patch version, normally:
0.1.1

unless an existing repository versioning policy requires another value.

Do not choose a major/minor bump merely because packaging was added.

If v0.1.0 does not actually exist in live repository/remote truth and the source-only release was never immutable/published, v0.1.0 may remain eligible.

Once chosen, define one canonical RELEASE_VERSION and make all release surfaces agree.

Surfaces include as applicable:
- VERSION
- CHANGELOG.md
- README.md release section
- release notes filename/content
- Windows VERSIONINFO
- package filenames
- installer metadata if an installer exists
- Git tag
- remote release title

No split identity such as:
VERSION=0.1.1
but binary metadata=0.1.0
or package name=v0.1.0.

Acceptance:
- one immutable release identity;
- previous tags untouched;
- version choice recorded with rationale.

======================================================================
MILESTONE C — OPEN THE BINARY RELEASE WORK THROUGH CANONICAL SAIPEN
======================================================================

The project is currently DONE with no active work.

Create/ingest the binary-release work through the canonical current SAIPEN mechanism.

Do not manually forge BOARD/STATE/LOG entries.

The release Work must cover this entire corridor.

Source intent:
official ProTrail Windows x64 binary release.

Acceptance must include:
- final icon integration;
- packaging;
- verification;
- release publication;
- post-publication verification.

Preserve cold-recovery continuity throughout the work.

======================================================================
MILESTONE D — FINAL PRODUCT ICON DEPENDENCY
======================================================================

Check whether the user-approved final ProTrail icon asset is already present in the live project or supplied alongside the execution context.

An approved asset must be distinguishable from:
- historical visual references;
- autogenerated development tray icons;
- temporary placeholders;
- arbitrary image files;
- AI-generated candidates that were never approved.

DO NOT self-approve a branding asset.

IF THE FINAL APPROVED ICON IS NOT AVAILABLE:

Continue all release-engineering work that can be completed safely without the icon.

Prepare:
- resource integration seam;
- deterministic packaging pipeline;
- metadata plumbing;
- tests;
- release verification scripts;
- artifact naming;
- documentation updates that do not falsely claim the binary is released.

Then checkpoint:

BLOCKED:
USER_PRODUCT_ICON_PENDING

Record exactly:
- required asset format;
- target path;
- accepted dimensions;
- whether a source PNG/SVG is also useful;
- next exact command/action after the asset is supplied.

Do not publish an official binary.

IF THE FINAL APPROVED ICON IS AVAILABLE:

Continue immediately.

Do not stop for confirmation.

======================================================================
MILESTONE E — WINDOWS BRANDING RESOURCE INTEGRATION
======================================================================

Integrate the approved icon as a real Windows application resource.

The application must not rely solely on a runtime QIcon call.

Provide a canonical Windows .ico resource with appropriate multi-resolution representations.

Target sizes should cover Windows shell/taskbar/tray usage appropriately, including common sizes such as:
- 16x16
- 20x20 if supported by the generation path
- 24x24 if supported
- 32x32
- 40x40 if supported
- 48x48
- 64x64
- 128x128
- 256x256

Do not manufacture low-quality stretched representations from a tiny raster source when a higher-quality master is available.

Integrate the icon into the Windows PE executable using the appropriate .rc / CMake resource path.

Also establish application icon authority for Qt surfaces:
- process/window/taskbar icon;
- Settings window;
- tray policy.

Avoid multiple independent icon authorities.

TRAY POLICY

Existing TrayIcon behavior currently uses generated development imagery.

Replace or adapt it deliberately.

Enabled and disabled states must remain distinguishable at actual Windows tray sizes.

Possible implementation:
- canonical brand mark + deterministic disabled-state treatment;
- separate approved state variants;
- another compact treatment consistent with the final design.

Do not make enabled/disabled differ only by an imperceptible subtle tint.

Tests should verify state authority and resource availability where practical.

======================================================================
MILESTONE F — WINDOWS VERSION RESOURCE / PRODUCT METADATA
======================================================================

Add or validate Windows VERSIONINFO metadata.

At minimum, where values are genuinely known:
- ProductName = ProTrail
- FileDescription = ProTrail
- FileVersion
- ProductVersion
- OriginalFilename = protrail.exe

Do not invent:
- company name;
- publisher identity;
- copyright ownership strings;
- legal entity details.

If those are absent, omit them or use only repository-established values.

Binary version metadata must derive from or be validated against the canonical release version.

Avoid manually duplicated version literals when a generated/configured source can safely provide one authority.

Acceptance:
- Explorer Properties displays correct version identity;
- PE icon is present;
- version matches VERSION/release tag.

======================================================================
MILESTONE G — RELEASE PACKAGING PIPELINE
======================================================================

The repository currently has deployment smoke infrastructure but no complete official packaging authority.

Create a deterministic Windows x64 portable-release pipeline.

MANDATORY first binary release artifact:

ProTrail-v<RELEASE_VERSION>-win-x64-portable.zip

Do not require a new installer architecture merely to satisfy this release.

An installer is OPTIONAL unless:
- one already exists canonically;
- repository policy requires one;
- the user has separately required one.

Do not explode scope by introducing MSI/MSIX/NSIS/WiX just because installers exist in the universe.

Humans have suffered enough.

PACKAGING PIPELINE REQUIREMENTS

Use a clean Release build.

Stage into a fresh dedicated release directory.

Deploy the required Qt runtime using the canonical Qt deployment tooling, normally windeployqt where appropriate.

The package must contain everything required to start ProTrail on a supported Windows x64 machine without the development Qt environment.

Verify required runtime surfaces including at least:
- protrail.exe
- Qt6Core.dll
- Qt6Gui.dll
- Qt6Widgets.dll
- platforms/qwindows.dll

Include additional Qt/runtime dependencies only when actually required.

Do not ship:
- build tree;
- object files;
- CMake cache;
- temporary logs;
- test executables unless intentionally published separately;
- developer-only configuration;
- source audit state;
- .saipen runtime machinery;
- unrelated PDBs in the user package;
- local user config;
- temporary smoke state;
- compiler/intermediate garbage.

If symbols are useful, publish them as a separate explicitly named developer artifact, not inside the ordinary portable package.

Make packaging reproducible enough that one command/script can produce the staging directory and archive from a clean checkout.

Prefer a PowerShell release script consistent with the existing Windows toolchain if no canonical packaging script exists.

======================================================================
MILESTONE H — PORTABLE PATH AND AUTOSTART REGRESSION MATRIX
======================================================================

Packaging changes the executable path and therefore interacts with ProTrail's autostart behavior.

Test deployed ProTrail from paths including:
- ordinary path;
- path containing spaces;
- nested path;
- non-repository deployment directory.

Where feasible, include a Unicode-path case.

Verify:
- startup succeeds;
- configuration location remains correct;
- logging location remains correct;
- executable-path discovery is correct;
- Start-with-Windows registration uses the actual deployed executable path;
- moving/redeploying the executable does not silently leave incorrect autostart state after the application's documented reconciliation path;
- no developer build path leaks into the packaged product.

Do not modify the operator's real production autostart state during tests without isolation/rollback.

======================================================================
MILESTONE I — CLEAN RELEASE BUILD
======================================================================

Use the documented toolchain unless live project policy has changed:

- Windows x64
- Visual Studio 2022 / MSVC
- Qt 6.8.x canonical project version
- CMake

Perform clean configure/build.

Release build must succeed with existing strict warning policy:
- /W4
- /WX
- zero errors
- zero warnings

Also build Debug if it remains a binding project gate.

Do not hide warnings by disabling /WX.

======================================================================
MILESTONE J — COMPLETE TEST GATE
======================================================================

Run the complete registered CTest suite.

Baseline snapshot contained 22 tests.

If the release work adds tests, the expected count may legitimately increase.

Require:
- Release: 100% PASS
- Debug: 100% PASS if current binding gate requires it
- no skipped mandatory tests
- no unexplained timeout
- no flaky retry-as-success games

Run focused release tests for:
- icon/resource presence;
- release version authority;
- deployment/package manifest;
- config;
- autostart;
- tray;
- unified GUI;
- single instance;
- request deduplication;
- render scheduler;
- multi-monitor;
- release defaults;
- lifecycle/shutdown.

======================================================================
MILESTONE K — DEPLOYED BINARY SMOKE
======================================================================

Use the deployed package, not build-tree protrail.exe.

Reuse and extend the existing:
- deploy.bat
- smoke_harness.ps1

The existing smoke already checks valuable invariants:
- process starts;
- required Qt DLLs exist;
- qwindows plugin exists;
- Settings window can be constructed/displayed;
- isolated smoke state is used;
- natural application shutdown occurs;
- exit code is zero;
- no deployment-specific orphan remains;
- production user state is not modified.

Keep those guarantees.

Add release-specific validation where necessary:
- packaged EXE has expected release version;
- executable contains the final Windows icon resource;
- package launches from a directory independent of source/build tree;
- no hidden dependency on developer environment paths;
- no unintended writable file is created beside the executable unless explicitly designed.

Smoke failure is a hard release failure.

No packaging after a failed smoke may be published.

======================================================================
MILESTONE L — CLEAN-MACHINE-LIKE DEPENDENCY CHECK
======================================================================

Prove that the release is not accidentally succeeding because the developer machine has Qt/MSVC junk on PATH.

At minimum:
- launch from the isolated deployment directory;
- use a sanitized/minimized PATH where practical;
- verify all required runtime DLLs resolve from the intended package/system locations;
- inspect the deployed dependency set.

If a suitable Windows sandbox/VM is locally available and safe to use, perform a smoke there.

Do not make VM availability an artificial blocker if the deterministic isolated deployment verification proves the intended dependency closure and project policy does not require a VM.

Document the confidence level honestly.

======================================================================
MILESTONE M — RELEASE ARTIFACT INTEGRITY
======================================================================

Generate the final portable ZIP only after the staged directory passes smoke.

Use deterministic, unambiguous artifact names.

Mandatory:
ProTrail-v<RELEASE_VERSION>-win-x64-portable.zip

Generate SHA-256 for every public binary artifact.

Create:
SHA256SUMS.txt

Verify the hashes after artifact creation.

Then independently extract the ZIP into a new temporary directory and rerun the deployed smoke against the EXTRACTED package.

This is mandatory.

Testing the pre-ZIP staging directory alone is insufficient.

Acceptance:
- staged package passes;
- ZIP created;
- ZIP hash recorded;
- ZIP extracts cleanly;
- extracted package independently passes smoke.

======================================================================
MILESTONE N — SIGNING POLICY
======================================================================

Inspect repository policy and available capabilities.

If an Authenticode certificate/signing service is already configured and authorized:
- sign the executable;
- sign installer if one exists;
- verify signatures;
- perform final packaging after signing;
- regenerate hashes after signing.

If no signing identity exists:
- do NOT invent one;
- do NOT use a self-signed certificate and present it as production trust;
- do NOT steal/use unrelated machine certificates.

Unless repository policy explicitly requires production code signing as a hard release gate, absence of a code-signing certificate is not by itself a reason to fabricate infrastructure.

Document the release as unsigned where appropriate.

Final product icon remains mandatory regardless of signing capability.

======================================================================
MILESTONE O — RELEASE DOCUMENTATION
======================================================================

Update release documentation to describe the actual binary release rather than the previous source-only state.

Surfaces:
- README.md
- CHANGELOG.md
- RELEASE_NOTES_v<RELEASE_VERSION>.md
- RELEASE_BLOCKERS.md
- CONTRIBUTING.md if its old source-only restriction is now obsolete

When the final icon gate is satisfied, retire:

FINAL_BINARY_RELEASE_BLOCKED: USER_PRODUCT_ICON_PENDING

Do not merely delete evidence of the blocker.

Record its resolution:
- approved asset integrated;
- Windows resource verified;
- tray/window policy verified;
- binary packaging gate green.

Release notes should state:
- supported platform;
- x64 architecture;
- portable package availability;
- major user-visible capabilities;
- installation/use instructions appropriate to a portable build;
- checksum availability;
- signing status if relevant.

Do not claim an installer exists when only a portable archive exists.

======================================================================
MILESTONE P — RELEASE SOURCE HYGIENE
======================================================================

Before committing:

Require a deliberate Git diff review.

Check:
- no local config;
- no build directory;
- no release staging directory;
- no smoke temp state;
- no private credentials;
- no signing secrets;
- no random AUDAPACK output;
- no generated binary committed unless repository policy explicitly tracks it;
- no obsolete MainWindow resurrection;
- no accidental SAIPEN runtime junk.

Reconcile the 11 tracked deletions reported by the supplied snapshot.

Some may be intentional.

Intentional deletions must be represented intentionally in the final commit.
Accidental deletions must be restored.

Reconcile the large untracked set.
Do not blindly `git add -A` before classification.

======================================================================
MILESTONE Q — FINAL SOURCE VALIDATION
======================================================================

Run all binding SAIPEN validation gates appropriate for the release task.

Require:
- core validation green;
- source/admission validation green;
- task verification green;
- review green;
- SHIP gate green;
- no stale blocker;
- no source receipt left half-consumed;
- no active audit inbox item relevant to the release;
- no unexplained P0/P1 finding.

If SubSaipen/HUNT produces a new hypothesis:
- independently reproduce it;
- do not accept it merely because a sub-agent emitted it;
- disposition it canonically;
- repair only confirmed release-relevant defects.

======================================================================
MILESTONE R — RELEASE COMMIT
======================================================================

Only after all pre-publication gates are green:

Create the canonical release commit.

The commit must contain only intended release source/config/docs/protocol changes.

After commit:
- working tree must be clean, excluding explicitly ignored/generated local release artifacts;
- rerun the minimum binding post-commit gate;
- record commit SHA.

Never release from an uncommitted product source state.

======================================================================
MILESTONE S — IMMUTABLE TAG
======================================================================

Create an annotated release tag:

v<RELEASE_VERSION>

ONLY if it does not already exist.

Never:
- force tag;
- move tag;
- delete remote tag to reuse a version;
- tag a different commit after artifacts were built from another commit.

The release artifact provenance must map exactly to the tagged commit.

Record:
- tag
- commit SHA
- artifact SHA-256

======================================================================
MILESTONE T — REPRODUCE ARTIFACT FROM TAGGED COMMIT
======================================================================

Before remote publication, prove provenance.

From the exact tagged commit or an equivalent clean checkout/worktree:

1. configure;
2. build Release;
3. run required tests;
4. package;
5. extract;
6. smoke;
7. hash.

The published artifact must derive from the tagged revision.

If this reproduction yields a materially different artifact due to uncontrolled inputs, investigate before publication.

Do not publish a ZIP built earlier from an ambiguous dirty tree.

======================================================================
MILESTONE U — PUSH
======================================================================

When gates remain green and repository authentication is available:

Push the release commit to the correct upstream branch.

Then push the immutable release tag.

Verify remote refs match local expected SHAs.

If authentication is unavailable:
- do not reinterpret that as product failure;
- checkpoint CAPABILITY_UNAVAILABLE with exact commands ready for execution;
- retain all locally validated release evidence;
- do not claim publication occurred.

======================================================================
MILESTONE V — REMOTE CI GATE
======================================================================

If remote CI is configured for the pushed revision/tag:

Observe the actual release commit CI result.

Current repository CI builds/tests Windows Release and Debug using Qt 6.8.0.

Require the release revision's relevant CI to pass before declaring public release DONE when remote CI is available.

A previous commit's green run is not evidence for the new release commit.

If CI reveals a defect:
- return to BUILD;
- repair;
- increment/recreate release identity only according to immutable-tag rules.

If the release tag was already pushed and the tagged commit is defective:
DO NOT move the tag.

Create the appropriate next patch release after repair.

======================================================================
MILESTONE W — PUBLISH RELEASE
======================================================================

If remote release publication capability is authenticated and available:

Create the official release for:
v<RELEASE_VERSION>

Attach:
- ProTrail-v<RELEASE_VERSION>-win-x64-portable.zip
- SHA256SUMS.txt

Attach additional legitimate artifacts only if intentionally produced.

Use the canonical release notes.

Do not attach:
- internal build directories;
- temporary smoke artifacts;
- source audits;
- unrelated logs;
- credentials;
- development executables.

The release must point at the exact immutable tag.

If a remote release for that tag already exists, inspect it rather than blindly overwriting it.

Never replace published assets silently if their content differs.

======================================================================
MILESTONE X — POST-PUBLICATION INDEPENDENT VERIFICATION
======================================================================

Do not declare success immediately after upload.

Verify the public/remote release surface.

Confirm:
- release exists;
- correct tag displayed;
- correct version displayed;
- portable ZIP downloadable;
- SHA256SUMS downloadable;
- published ZIP hash matches local canonical hash;
- asset name correct;
- release notes correct;
- no obsolete source-only blocker text falsely remains;
- no accidental extra assets;
- remote tag commit matches release commit.

If possible, download the published portable ZIP into a new directory and:
- verify SHA-256;
- extract;
- execute release smoke.

This closes the loop between local artifact and actual published artifact.

======================================================================
MILESTONE Y — FINAL SAIPEN CLOSURE
======================================================================

After publication verification passes:

Record final executable evidence through canonical SAIPEN operations.

The release Work may finish only when evidence proves:

SOURCE
- canonical repository state committed;
- release source clean;
- release version consistent.

BUILD
- clean Release build green;
- warning policy green.

TEST
- complete required CTest gate green.

BRANDING
- approved icon integrated;
- PE icon verified;
- window/taskbar icon verified;
- tray enabled/disabled policy verified.

PACKAGE
- Qt dependency closure verified;
- portable ZIP created;
- extracted ZIP smoke PASS.

INTEGRITY
- SHA-256 published and verified.

GIT
- release commit exists;
- immutable release tag exists;
- remote refs match.

CI
- relevant remote release revision green when CI is available.

PUBLICATION
- official release exists;
- public assets match canonical hashes.

Then:
- finish the release ticket canonically;
- STATE may return to DONE;
- BOARD must contain no hidden unfinished release work;
- blocker must be none unless an unrelated future issue remains.

======================================================================
FINAL ACCEPTANCE CRITERIA
======================================================================

The release is DONE only if all applicable statements are true:

[ ] Final user-approved ProTrail icon exists.
[ ] Icon is integrated into Windows executable resources.
[ ] Window/taskbar icon is correct.
[ ] Tray enabled/disabled states remain recognizable.
[ ] One canonical release version is used everywhere.
[ ] Existing historical tags were not rewritten.
[ ] Release source tree was canonicalized.
[ ] Release commit was produced from reviewed source.
[ ] Clean Release build passed with /W4 /WX.
[ ] Required Debug gate passed if binding.
[ ] Full CTest suite passed.
[ ] Portable package includes required Qt runtime.
[ ] Package runs outside build/source tree.
[ ] Existing deployment smoke passes.
[ ] Extracted final ZIP independently passes smoke.
[ ] Real user configuration/state is untouched by smoke.
[ ] No orphan protrail.exe remains after smoke.
[ ] Autostart executable path behavior remains correct.
[ ] Public artifact SHA-256 is recorded.
[ ] Signing state is represented honestly.
[ ] Immutable release tag maps to release commit.
[ ] Published release maps to that tag.
[ ] Published artifact hash matches canonical local hash.
[ ] Release documentation describes the actual release.
[ ] USER_PRODUCT_ICON_PENDING is canonically resolved.
[ ] Final SAIPEN release task is terminal.
[ ] Project returns to clean DONE state.

======================================================================
NON_GOALS
======================================================================

Do NOT:
- redesign the ProTrail UI;
- resurrect MainWindow architecture;
- rewrite Trail/Sparkle/Click/Hold effects without a reproduced defect;
- add unrelated features;
- change release defaults casually;
- introduce an installer framework solely for prestige;
- add telemetry;
- add an updater;
- add account/cloud infrastructure;
- redesign SAIPEN;
- reopen closed audit findings without evidence;
- force-push;
- overwrite tags;
- publish from dirty source;
- fabricate branding;
- fabricate signing identity.

First binary release should remain deliberately boring and dependable.

Portable ZIP + verified runtime + checksums is sufficient.

======================================================================
BLOCKER BEHAVIOR
======================================================================

Use fail-open progression for recoverable release engineering.

If one optional capability is unavailable, continue all independent eligible work.

Examples:

NO FINAL ICON
- complete all safe release preparation;
- stop before official binary publication;
- checkpoint USER_PRODUCT_ICON_PENDING.

NO CODE-SIGNING CERTIFICATE
- continue unsigned if signing is not a canonical hard requirement;
- document status honestly.

NO VM
- perform isolated local package verification;
- do not invent VM evidence.

NO REMOTE AUTH
- complete local release candidate, tag preparation and evidence;
- checkpoint CAPABILITY_UNAVAILABLE for publication only.

REMOTE CI FAILURE
- treat as real release blocker;
- investigate and repair.

PRODUCT TEST FAILURE
- return to BUILD;
- fix root cause;
- rerun complete affected gates.

TAG ALREADY EXISTS
- never rewrite it;
- select the next valid release version.

======================================================================
CHECKPOINT / CONTEXT EXHAUSTION RULE
======================================================================

Before context exhaustion, interruption, tool restart, or voluntary stop, persist a cold-recovery checkpoint containing:

- current release version;
- release ticket;
- current phase;
- live HEAD;
- branch/upstream;
- dirty/clean state;
- final icon status;
- files changed;
- build results;
- test counts/results;
- packaging status;
- staged artifact path;
- artifact SHA-256;
- signing status;
- tag status;
- push status;
- CI status;
- remote release status;
- exact blocker if any;
- next exact executable action.

Never leave the next agent needing the previous chat to determine release truth.

======================================================================
AUTONOMY RULE
======================================================================

Continue autonomously while eligible work remains.

Do not stop after:
- icon plumbing;
- a successful build;
- packaging;
- tests;
- commit;
- tag;
- push;
- CI;

if the next release milestone is executable.

The intended terminal state is:

OFFICIAL PROTRAIL WINDOWS RELEASE PUBLISHED AND VERIFIED
+
SAIPEN DONE
+
CLEAN RELEASE SOURCE
+
IMMUTABLE PROVENANCE

If the final approved icon is the only remaining dependency, finish every independent preparatory milestone first and stop at that single precise boundary rather than inventing more work.
