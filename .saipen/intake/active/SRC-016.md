# User request

priority: P1
verify: each clause is present and demonstrated against the user own description of it; the source-only release contains no binary artifact and the product-icon blocker stays open

## Request

Canonicalize the completed unified ProTrail UI and repair protocol drift, then finish source-only v0.1.0 ship.

The current canonical source receipts stop at SRC-011, which describes the superseded model: a compact Main Essentials window plus a separate Advanced SettingsWindow.

The user's later, controlling requirement is already implemented in source but has not been canonically ingested. Ingest it now as the authoritative product specification and supersede the split-window portions of SRC-011 while preserving SRC-011's GitHub/source-release work, canonical defaults work, icon blocker, and binary-release restrictions.

ONE PRODUCT WINDOW

- The product has exactly one user-facing window named ProTrail.
- That window owns General, Trail, Click, and Developer tabs.
- Developer appears only in developer builds.

NO:
- no separate MainWindow;
- no Advanced Settings second window;
- no duplicated editor surfaces;
- no ordinary style/mode QComboBox UI for style, mode, or easing selection.

General:
- Master FX
- Trail FX
- Click FX
- Start with Windows
- quick color presets
- Restore All Defaults

Trail:
- complete Trail/Sparkle controls in a scrollable tab

Click:
- complete Click/Hold/Motion Wake controls in a scrollable tab

Developer:
- canonical developer presets/default authoring
- Set Current as Release Defaults from the complete canonical application configuration

Lifecycle:
- manual launch -> unified window on General
- autostart -> tray-only, no product window
- tray / interactive second instance -> same unified window
- close -> hide to tray
- explicit Exit -> process termination

This receipt explicitly supersedes the split-window portions of SRC-011 while preserving SRC-011's:
- GitHub/source-release work;
- canonical defaults work;
- icon blocker;
- binary-release restrictions.

Do not rewrite or delete SRC-011 history.
