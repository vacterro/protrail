# Log

- 17.09.26 18:20 [HUNT-001] RUN: adopt -- spawned by the crew circuit (stage SC-1 instance assurance), then re-adopted here on the SC-2 RUN_ROLE carrier. Read order 1-5 completed: own STATE/BOARD/LOG (empty instance), project-local PROTOCOL.md, project-local saihunt.md charter, the six-signal source surfaces, and `.saipen/BOARD.md` to check nothing is already tracked. Recorded role_revision sha256:4edb04181cb07e0946afd06fbe711166fa9dcc403e56b52e9be3844f0a71b0a5.
- 17.09.26 18:22 [HUNT-001] RUN: scout -- signals run against this tree: stale markers in src/ and tests/; config save/load key symmetry in src/config/config_storage.cpp; silent-failure paths (ignored error_code, discarded results) across src/; orphan source files by reference count; unverified commits against `.saipen/LOG.md`; and the test suite state. Already-known silent-failure findings T-38..T-42 on the main BOARD cover the config `.corrupt` backup, the autostart registry reads, log directory creation and the single-instance ResetEvent results, so those were checked for a NEW instance of the same class rather than re-filed.
- 17.09.26 18:23 [HUNT-001] RUN: signal 3 (stale markers) -- CLEAN. `grep -rn 'TODO\|FIXME\|HACK\|XXX' src tests --include=*.cpp --include=*.h` returns 0 hits. No finding.
- 17.09.26 18:23 [HUNT-001] RUN: signal 5 (symmetry) -- CLEAN for the persisted configuration. Extracting `obj["key"]` from `serialize_json` and `contains("key")`/`["key"]` from `deserialize_json` gives 50 keys written and 50 read with BOTH directional differences empty. This is the surface a new setting is most likely to break, so the result is worth stating: T-032's `start_with_windows` participates fully in both directions.
- 17.09.26 18:23 [HUNT-001] RUN: signal 6 (dead code/orphans) -- CLEAN. No file under src/ has zero references from the rest of src/, tests/ or CMakeLists.txt.
- 17.09.26 18:24 [HUNT-001] RUN: signal 4 (silent failures) -- inspected and NOT filed: `config_storage.cpp:419` (`create_directories` result discarded) is covered by the following `out.is_open()` check on the same path; `config_storage.cpp:434` and `:451-459` check or return on their error paths; `overlay_window.cpp:762` discards `trail_batch_.admit()`, whose return the caller re-derives from `pending()` one line above, so nothing is lost. The class is already represented by T-38..T-42.
- 17.09.26 18:25 [HUNT-001] RUN: signal 1 (tests) -- TOOLING BLOCK, recorded honestly. Running the suite through ctest was attempted twice and both invocations ended with no transcript and no child process; the cause is the agent shell's `cmd /c "<command with embedded quoting>"` form starting an INTERACTIVE interpreter that waits on stdin, not the project. That trap is why the first reading of this signal looked like a 600-second hang. Signal 1 was then answered without ctest: each non-intrusive executable was run directly with Qt's bin on PATH and QT_QPA_PLATFORM=offscreen, per-test transcripts under kitchen/logs/. Result: 17/17 PASS in Release and 17/17 PASS in Debug, `protrail_input_dispatch` never invoked.
- 17.09.26 18:26 [HUNT-001] RUN: signal 2 (unverified commits) -- no finding. The repository has a single commit (`1fc09f0`); the uncommitted T-032 autostart work in the working tree is already represented by LOG events in the main project's own log, so no commit is silently unverified.
- 17.09.26 18:27 [HUNT-001] RUN: verdict on a suspected failing test (NOT_REPRODUCED) -- `build/Testing/Temporary/LastTestsFailed.log` names `8:protrail_hold_wake_tests`, which reads as a live Debug failure and contradicts the session's green claim. Tried: (a) the same directory's `LastTest.log:113-127` records test 8 as Passed in the last recorded run; (b) `build/Debug/protrail_hold_wake_tests.exe` run directly reports 44813 checks 0 failures exit 0; (c) the Release executable reports the same. The artifact is stale evidence from an earlier run, and no ticket is warranted -- but a reader who trusts `LastTestsFailed.log` without running the test would file a phantom.
- 17.09.26 18:28 [HUNT-001] RUN: FINDING (REPRODUCED, P3) -- no CTest target declares a TIMEOUT. `grep -c TIMEOUT CMakeLists.txt` returns 0; the five `set_tests_properties` blocks (lines 117, 243, 364, 392, 464) configure ENVIRONMENT and labels only; and `ctest_safe.bat` runs ctest without `--timeout`. The absence is the proven fact and the hang it permits is a consequence, not something reproduced here -- stated that way in the entry itself so the severity is not read as an observed outage. The suites most exposed are the ones that can wedge on device or platform faults: tray, multi-monitor and the ~53 s Debug GUI run. Filed as HUNT-001 through kitchen/OUTBOX.md.
- 17.09.26 18:29 [HUNT-001] RUN: prepare saihunt -> done -- one complete package written to kitchen/OUTBOX.md bound to the source identity above, HUNT-001 moved to ## DONE. No other entry: the clean signals are recorded in this log rather than dressed up as findings, because a finding has to carry what breaks.
- 19.09.26 23:01 [HUNT-002] RUN: crew SC-2 bounded current-source scan -- six signals rerun against source identity 1fc09f09410da3493ed8f7ffca621ca0467ca259 + git-delta-v1:f74afc7dc0722faaf697148c627eaa6b3ee1bbac78c5b4ea83ed18a87dc5286a; clean on stale markers, config symmetry, inspected silent failures and orphans; REPRODUCED P3 finding remains no finite CTest TIMEOUT, packaged in OUTBOX; HUNT-001 marked stale.
- 19.09.26 23:03 [HUNT-002] DEC: package stale after Core repaired CMakeLists.txt timeout properties; T-48 owns the terminal blocked verification state, no re-collection.
