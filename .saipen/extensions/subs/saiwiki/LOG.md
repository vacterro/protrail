# Log

- 16.09.26 10:56 [W-001] RUN: adopt -- read order 1-5 completed: own STATE/BOARD/LOG, project-local PROTOCOL.md, project-local saiwiki.md charter, then the canonical ID sources. Recorded role_revision sha256:54a42475a124ab0f27e83d600a284a9cc54d9668029c4828cfc48512b031df13 from the local charter (verified equal to the derived digest via tools/freshness.compute_role_revision).
- 16.09.26 10:57 [W-001] RUN: scout -- canonical ID sources in this project are .saipen/BOARD.md (T-### tickets, 27 contiguous), .saipen/intake (SRC-### receipts, 4), ROADMAP/roadmap/{mvp,future} (milestone IDs 0-21), the persisted style enums in src/effects/{click,trail}_config.h, the schema boundary constants in src/config/app_config.h, and the CTest registrations in CMakeLists.txt. Item 4 of the read order names "CONFORMANCE.md's id-to-title map"; that file does not exist in this project -- filed separately as W-002 rather than guessed.
- 16.09.26 10:58 [W-001] RUN: prepare saiwiki -- source identity bound: source_head 1fc09f09410da3493ed8f7ffca621ca0467ca259, source_tree_fingerprint git-delta-v1:141993d497baeef64542d11824983209e6ee245350cbbcdd88f6b399e053c051, discovery model git-delta-v1 (tools/freshness.compute_source_identity).
- 16.09.26 10:59 [W-001] RUN: build -- eleven pages drafted into kitchen/wiki/, one per canonical ID space, each row-set page carrying its own `mirrors: <source> rows <lo>-<hi> sha256:<16>` marker over the projection it declares. No prose re-transcribed from the canonical sources; compacted board records are pointed at, not copied.
- 16.09.26 11:00 [W-001] RUN: verify -- VERIFY phase. Suite: kitchen/_verify_wiki.py (scratch, `_`-prefixed). Result: pages=11 checks=28 failures=0, WIKI_VERIFY_RESULT PASS.
- 16.09.26 11:00 [W-001] RUN: verify red control -- the first verifier compared only each marker's digest against a fresh digest of the canonical source, and passed with a genuinely corrupted Tickets.md row on the page. That is a gate that cannot fail, so it was replaced. The current verifier re-derives the projection AND parses the page body back into rows, comparing cell-by-cell by EQUALITY. Red proven end to end: changing T-005's title from "README build section, ..." to "README section, ..." produced exactly one failure naming row 5; reverting returned the run to PASS. Every page additionally runs a per-run known-bad control on its parsed page rows.
- 16.09.26 11:01 [W-001] RUN: verify freshness -- source_head and source_tree_fingerprint re-computed AFTER all writes and unchanged. The wiki lives under .saipen/, which the fingerprint excludes by construction, so a producer cannot invalidate its own package.
- 16.09.26 11:01 [W-002] RUN: blocked -- the charter's output_contract and read order item 4 name a canonical source (CONFORMANCE.md, tests/conformance_cases.jsonl) that this project does not have. Per PROTOCOL.md § 2 an undeterminable fact is a `blocked` OUTBOX entry, never a guess; also recorded as W-002 on this board with the exact missing fact.
- 16.09.26 11:02 [W-001] RUN: prepare saiwiki -> done -- package written to kitchen/OUTBOX.md as status ready, bound to the source identity and role revision above, W-001 moved to ## DONE. Package covers the whole maintained wiki, not a sampled page.
- 17.09.26 14:28 [W-001] RUN: adopt (re-adoption, `qq`) -- the recorded package was STALE on evidence, not on age: the project had moved to 42 tickets, 5 receipts, schema boundary 10 and 18 CTest targets, and the working-tree delta had changed (source_tree_fingerprint 141993d4... -> 6cad7aa6...; source_head unchanged at 1fc09f0...). PROTOCOL.md § 6 classifies that as stale evidence, so the package was rebuilt rather than edited.
- 17.09.26 14:29 [W-001] RUN: scout (projection audit) -- two verifier projections turned out to have a hard-coded SCOPE: source_rows() iterated range(1, 5) and schema_rows() named four boundary constants in a literal tuple. Both pages reported GREEN while SRC-005 and kStartWithWindowsSchema = 10 were invisible to them, and a third page was stale in a way the gate could not phrase: heading and marker had drifted together (both claimed 1..17 against 18 registered targets). A projection whose scope is a literal list is a second source of truth, so both now derive their ID set from the project's own files, a derived gap FAILS instead of being skipped, and every heading that restates a range is checked against its marker.
- 17.09.26 14:29 [W-001] RUN: build -- added kitchen/_regen_wiki.py (scratch): re-derives every mirrored table, each marker, the range-carrying headings, the ticket counts, the intrusive-target arithmetic, the current-schema sentence, the package binding and the index page list, and applies the authored prose edits whose facts changed. It imports the projections from _verify_wiki.py rather than re-implementing them and asserts its own richer parse reproduces them. Table row counts: Tickets 27 -> 42, Sources 4 -> 5, SchemaBoundaries 6-9 -> 6-10, TestSuites 17 -> 18.
- 17.09.26 14:30 [W-001] RUN: build (defect found and repaired) -- the first _regen_wiki.py replaced the FIRST table on Index.md, which is the package-binding table, and wrote the page list over it. It also applied insertion-style prose edits unconditionally, so a second run duplicated them (the replacement contains the anchor; testing the anchor first is not idempotent). Both were fixed: the index table is addressed by its own heading, and prose edits check the replacement first and collapse a doubled insertion rather than adding a third copy. Modules.md/SchemaBoundaries.md/Sources.md lost one duplicated insertion each and the second run reports every page unchanged.
- 17.09.26 14:30 [W-001] RUN: verify -- VERIFY phase. Suite: kitchen/_verify_wiki.py. Result: pages=11 checks=32 failures=0, WIKI_VERIFY_RESULT PASS (checks rose from 28 to 32 with the new heading-range checks). Four formerly-invisible rows are now checked.
- 17.09.26 14:31 [W-001] RUN: verify red control -- added kitchen/_red_control.py (scratch): it corrupts a REAL row on a REAL page, runs the verifier as a subprocess, requires exactly that page and row to be named, then restores the byte-identical original. Result: controls=4 failures=0, red on Tickets row 40, Sources row 5, SchemaBoundaries row 10 and TestSuites row 18 -- precisely the four rows the old hard-coded projections could not see -- and the wiki returned to PASS with every page byte-identical.
- 17.09.26 14:31 [W-001] RUN: verify freshness -- head 1fc09f09410da3493ed8f7ffca621ca0467ca259 and fingerprint git-delta-v1:6cad7aa60e33f8538ad4ad6c2f1aae1e4c7d00a99d8a4591425097a00247080a re-computed AFTER every write and unchanged.
- 17.09.26 14:32 [W-001] RUN: publish -- found that `kitchen/OUTBOX.md` alone is NOT what the targeted path consumes: `saipen collect` is not even a command in this engine version, and the complete-package path scans `<namespace>/READY/*.json` via producer.StagingGeneration.scan_ready (the `qqq` macro drives it). Added kitchen/_publish_ready.py (scratch) which stages and publishes through the engine's OWN API rather than hand-writing JSON -- 33 declared read sources revalidated at publish, 11 payload files, epoch 0 -- and got {'ok': True, 'code': 'PUBLISHED', 'package_identity': 'sha256:f5aa190ca57985a61352a57c753e28f493dcc6c516eee7b74b58d09f120bb79e'}. scan_ready now reports 1 package, 0 errors, status ready; the staging generation was removed by publication.
- 17.09.26 14:32 [W-002] RUN: answered -- the missing fact was one directory away, in saipen_home: CONFORMANCE.md does exist there, and tests/conformance_cases.jsonl does NOT exist there either, so that half of the read order is unsatisfiable even in the repository that wrote it. `saipen sub spawn` copies built-in charters verbatim by design, and this project's own `Conformance: PASS` is Core's receipt ledger, a different artifact. The chore is inherited wording, not a missing deliverable; status moved from blocked to ready with the decision (correct the charter wording, or ticket a CONFORMANCE.md) left explicitly to Core. Nothing in W-001 depends on it.
- 17.09.26 14:32 [W-001] RUN: prepare saiwiki -> done -- READY package sha256:f5aa190c... plus the refreshed OUTBOX binding, W-001 and W-002 both in ## DONE on this board. Package covers the whole maintained wiki, not a sampled page.
- 17.09.26 14:33 [W-001] RUN: verify (line endings) -- the four red-control pages came back LF: the control restored text it had read through newline normalization, and _regen_wiki.py compared a normalized read too, so a wrong line convention was invisible to both. Both tools now work at the byte level (the control restores the exact original bytes and FAILS if a page it touched is left LF; the generator compares raw bytes). All eleven pages normalized to CRLF and _red_control.py's four corruptions now restore byte-identically.
- 17.09.26 14:35 [W-001] RUN: publish (republish) -- the first publication carried LF payload bytes, so it was stale the moment the pages were normalized. Republished: package_identity sha256:0ed06f0353b37c67467fff669c7f7fd72129d8bf00e638c5e1c8522d6fab5257, and the earlier sha256:f5aa190c... artifact was moved to SETTLED/ by the engine's own retirement path rather than deleted.
- 17.09.26 14:35 [W-001] RUN: publish (defect found and repaired) -- the retirement step was written to clear superseded evidence and walked EVERY file in READY/, so it moved the package it had just published into SETTLED/ too: scan_ready reported 0 packages. Fixed to skip the identity just published. The mis-retired duplicate was then removed, because a SETTLED copy of a live identity contradicts it; the genuinely superseded artifact remains in SETTLED/. Re-published state: exactly 1 READY package, 0 errors.
- 17.09.26 14:40 [W-001] RUN: verify (OUTBOX grammar) -- `sub status` returned health INVALID: I had added two fields the closed grammar does not allow (`package_identity`, `ready_artifact`), left `verified` as multi-line prose where the rule is one-line `PASS -- <evidence>`, and filed W-002 as a second entry. The rule read from the engine source is categorical: every non-legacy entry must carry OUTBOX_COMPLETE_FIELDS whatever its status, and only `legacy: true` exempts one. A finding therefore cannot be filed in the OUTBOX at all, so W-002 moved to `_shared/inbox.md` (the append-only finding surface PROTOCOL.md section 4 names) and the identifying fields folded into W-001's payload/details where the grammar allows prose. This is a better shape than the original: one package, one door, no entry padded with provenance it does not have.
- 17.09.26 14:40 [W-002] RUN: disposed -- moved out of the OUTBOX to `_shared/inbox.md` as an appended finding line, with its answer also on wiki/Index.md and on this board. Status is now neither `ready` (it has no payload and no source binding) nor `blocked` (the missing fact was found): it is a dispositioned finding awaiting a Core decision, which is exactly what the inbox surface is for.
