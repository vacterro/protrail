# OUTBOX

## SAIT-001: Translation surface package for ProTrail (README docs surface + UI-string inventory)

- **status:** draft
- **summary:** force-fresh saitranslate preparation over the current source: the README docs surface is re-verified and digest-bound for the default set (English, Russian, Estonian, Ukrainian, Japanese, Дед); the remaining 27 locales and the UI-strings surface are named as explicit gaps, so this package is `draft` and MUST NOT be collected yet.
- **main_project_refs:** [README.md, src/ui/settings_window.cpp, src/ui/tray_icon.cpp, src/app/application.cpp, src/app/main.cpp]
- **critical:** false
- **severity:** P2
- **producer:** saitranslate
- **source_head:** 1fc09f09410da3493ed8f7ffca621ca0467ca259
- **source_tree_fingerprint:** git-delta-v1:141993d497baeef64542d11824983209e6ee245350cbbcdd88f6b399e053c051
- **role_revision:** sha256:f241e6b83c39e9b46bfa586638efb0374bbb39889646f723b9189bbb4912c0c5
- **coverage:** surface 1, user-facing documentation: README.md (4875 bytes, digest sha256:0b904f769d59910f, no version string anywhere so the required version-to-VERSION normalization is a genuine no-op, verified by grep rather than assumed). Delivered locales 6 of 33: en, ru, et, uk, ja, ded. Each carries `<!-- source-digest: README.md sha256:0b904f769d59910f -->` matching the current README. Pending locales 27 of 33: de fr es it pt nl pl sv da fi no zh ko th vi ar he tr hi id el cs ro hu bg sk hr. Surface 2, real in-app UI strings: PRESENT (37 English literals across src/ui/settings_window.cpp, src/ui/tray_icon.cpp) but NOT COVERABLE by this producer -- ProTrail ships no locale layer (no .ts, no .qm, no QTranslator anywhere in src/), so there is no existing locale format to fill and phases/translate.md forbids inventing keys or a bundle. Evidence only, never integratable.
- **payload:** exact files to integrate when this package becomes ready (paths are inside this package; the right column is the main-tree destination): `mirrors/README.ee.md` -> `README.ee.md` (Estonian, contract-named root mirror); `mirrors/README.ded.md` -> `README.ded.md` (Дед voice, contract-named root mirror); `mirrors/README.ja.md` -> `README.ja.md` (Japanese, contract-named root mirror); `mirrors/README.ru.md` -> `README.ru.md` (Russian, sibling convention plus language switcher, NOT contract-named); `mirrors/README.uk.md` -> `README.uk.md` (Ukrainian, same sibling convention); `locales/en/README.md` and `locales/{ru,et,uk,ja,ded}/README.md` are the translation surface of record and are never main-tree destinations; `evidence/verification.txt`, `evidence/file-digests.txt`, `evidence/ui-strings-en.json` are read-only evidence, never integratable. All five root mirrors are currently ABSENT from the main tree, so integration is a clean add; the only main-tree file this package never replaces is `README.md` itself, which is the source.
- **verified:** PASS -- python kitchen/tools/verify_package.py . kitchen/evidence/verification.txt -> VERIFY: PASS (0 failing surface(s)), VERDICT: draft (default set only, 27 locales remain); every delivered locale carries <!-- source-digest: README.md sha256:0b904f769d59910f --> and that value equals sha256(README.md)[:16] computed in the same run; every delivered mirror is byte-identical to its locale file; python kitchen/tools/extract_ui_strings.py . kitchen/evidence/ui-strings-en.json -> ui strings: 37 literals from 4 files.
- **instructions:** ordered, for the later `eee` collection once this entry is ready. 1. Read evidence/verification.txt; refuse collection unless it reports VERIFY: PASS and delivered locales: 33/33. 2. Re-check the freshness triple against the tree: source_head, source_tree_fingerprint, role_revision. Any mismatch makes this package stale, not collectable. 3. Apply the five mirrors/* files to the main-tree root paths named in payload, verbatim. 4. Leave README.md untouched -- it is the source, never a payload target. 5. Do not integrate anything under locales/ or evidence/; those are the producer's surface of record and read-only evidence. 6. Open a Core ticket for the UI-string surface only if the project decides to add a locale layer (.ts plus QTranslator); that work is an i18n feature ticket, not a translation delivery.
- **details:**
  **Why draft and not ready.** phases/translate.md permits `status: ready` only for complete 32-language-plus-Дед coverage of every real surface. 6 of 33 locales plus an uncoverable UI surface is honest partial coverage, so the package stays `draft`, the 27 pending locales are named individually in evidence/verification.txt, and the collect path must refuse it.
  ---
  **What this force-fresh run actually changed, and what it did not.** The source tree fingerprint moved from git-delta-v1:223de575... to git-delta-v1:141993d4... because the project changed between the two runs, so the previous package was stale by identity and had to be re-bound. The translated prose itself did NOT change and was not rebuilt: all eleven payload documents are byte-identical to the 12.09.26 run, recorded in evidence/file-digests.txt, because README.md is byte-identical. phases/translate.md forbids rebuilding unchanged bytes or stamping an untouched locale as refreshed, so nothing was re-translated and nothing is claimed as refreshed.
  ---
  **Real drift found in the second surface.** The UI-string inventory moved from 33 literals to 37: src/ui/settings_window.cpp went from 27 to 31 and src/ui/tray_icon.cpp held at 6. The four new literals are the HOLD controls added by the T-026/T-027 work (`HOLD`, `Hold FX`, `Motion Wake` and the section's fourth label). This is genuine drift, and it is reported rather than absorbed -- the surface still has no integratable form, so it remains a Core i18n decision.
  ---
  **Package hygiene corrected in this run.** The previous entry was headed `## W-001`, which is saiwiki's identifier namespace; PROTOCOL.md section 3 assigns saitranslate the `SAIT-` prefix, so two producers could have emitted a colliding W-001. It is now `SAIT-001`. Its `verified:` field was also bullet prose, which is not a closed verdict and made `saipen sub list` report health=INVALID for this instance; it is now a single `PASS -- <command/result>` line. The package body also had to be reformatted to the closed OUTBOX grammar, which permits no blank line inside a field.
  ---
  **Open issue, deliberately not resolved here: this package sits where `eee` will not look.** phases/translate.md and the engine's `producer.producer_namespace()` both place saitranslate's namespace at `.saipen/saitranslate`, while the shipped charter's `write_scope` and the engine's `subs._sub_dir()` used by `sub spawn` both use `.saipen/extensions/subs/saitranslate`. This instance is at the charter's path. The consequence is that `saipen prepare saitranslate` cannot complete: it resolves its own namespace as absent, calls sub_spawn, and sub_spawn refuses ALREADY_CLAIMED against the path that does exist. `saipen collect saitranslate` would likewise read the empty canonical path and see no package. The engine's own escape hatch is closed too -- `saipen sub clean saitranslate` refuses on the draft OUTBOX and 17 unacknowledged artifacts. Core was asked and chose to refresh in place at the charter's path rather than relocate the instance, so this disagreement is recorded here and remains open; it is an engine defect, not a ProTrail defect, and nothing in this package can fix it.
  ---
  **Ownership note, per the charter's authority table.** Core owns EN/EE/RU/DED and this producer owns the other 29. The et, ru and ded documents are delivered because the TRANSLATE phase requires the default set to exist and be current; they are prepared for Core, not claimed as this producer's property. Drift found in them is reported here, never silently rewritten.
  ---
  **Boundary disclosure.** Every file this producer wrote is inside `.saipen/extensions/subs/saitranslate/`. The main tree was read-only for this run; no main-tree byte was touched, and the five root mirrors the payload names are still absent from it.

## SAIT-002: saitranslate cannot be prepared or collected through the engine

- **status:** blocked
- **summary:** `saipen prepare saitranslate` cannot complete, and `saipen collect saitranslate` would never see this instance's output: the engine resolves the producer namespace to `.saipen/saitranslate` while the shipped charter's write_scope, the engine's own spawn path, and the live instance are all `.saipen/extensions/subs/saitranslate/`.
- **main_project_refs:** [.saipen/extensions/subs/saitranslate.md, .saipen/extensions/subs/saitranslate/]
- **critical:** false
- **severity:** P2
- **producer:** saitranslate
- **source_head:** 1fc09f09410da3493ed8f7ffca621ca0467ca259
- **source_tree_fingerprint:** git-delta-v1:141993d497baeef64542d11824983209e6ee245350cbbcdd88f6b399e053c051
- **role_revision:** sha256:f241e6b83c39e9b46bfa586638efb0374bbb39889646f723b9189bbb4912c0c5
- **coverage:** the engine prepare/collect path for this producer, and the charter and phase documents that disagree with it; no wiki or locale page depends on this entry
- **payload:** none -- this entry reports a fact and asks a question, it produces no artifact
- **verified:** BLOCKED -- missing fact: which authority wins for saitranslate's on-disk namespace -- the shipped charter's `write_scope: ".saipen/extensions/subs/saitranslate/"` and the engine's `subs._sub_dir()`, or phases/translate.md and the engine's `producer.producer_namespace()`, which both say `.saipen/saitranslate`
- **instructions:** Core chooses which authority wins and then fixes the loser. If the charter is wrong, correct its write_scope and required read order and relocate this instance with a reference sweep. If the engine is wrong, `subs._sub_dir()` must honour `producer.PRODUCERS` the way `intent._role_dir()` already does, and `saipen sub clean` must remain the only path that removes an instance. Either way the fix lives outside this project's tree. Do not delete or relocate this instance from inside the project; its draft package is uncollected work.
- **details:**
  **The mechanism, exactly.** `saipen prepare saitranslate` takes the producer branch, resolves `intent._role_dir()` through `producer.producer_namespace()`, which special-cases saitranslate to `.saipen/saitranslate` -- a path that does not exist here -- so it calls `subs.sub_spawn()`. `sub_spawn` resolves its target through `subs._sub_dir()`, which is always `.saipen/extensions/subs/<name>` and therefore does exist, and refuses `ALREADY_CLAIMED` with the message "subSaipen 'saitranslate' already exists". `intent` then maps that to `PRODUCER_SPAWN_FAILED`, so the run ends in a hard failure instead of the `ROLE_NOT_RUN` routing instruction a producer role is supposed to emit. `prepare --dry-run` corroborates it: "would spawn saitranslate instance".
  ---
  **The escape hatch is closed by the engine's own gate.** `saipen sub clean saitranslate` refuses: "OUTBOX status draft" plus 17 "unacknowledged artifact" blockers covering every file in this kitchen. So the path that blocks the spawn cannot be cleared from inside the project, and nothing in this package can fix that.
  ---
  **Consequence for `collect`.** `saipen collect saitranslate` reads the producer namespace, so it would look in the empty `.saipen/saitranslate` and find nothing, regardless of what this kitchen contains. The SAIT-001 package is therefore correct and verified but unreachable through the engine until this is settled -- which is why it is `draft` and why this entry exists rather than a claim that the delivery path works.
  ---
  **What was NOT done.** No instance was relocated, no file was deleted, no MANIFEST entry was edited, and the engine was not modified. Core was asked which authority wins, and Core chose to refresh in place at the charter's path rather than relocate the instance, so this remains open by explicit decision rather than by oversight.
  ---
  **Charter-versus-tool drift, same class the protocol already documents.** PROTOCOL.md section 1 records that its own phase ban once "said the stricter thing and the tool did the workable thing". This is that shape again: the shipped charter and the engine's spawn path agree with each other and disagree with the phase document and the engine's producer namespace. It is reported here rather than resolved by picking the more convenient side.
