# User request

priority: P1
verify: two clicks at different positions/times produce visibly different particle layouts; same bubble renders identically at any frame cadence; monotone outward travel preserved; CTest green

## Request

Burst/SparkBurst per-click randomization: click_bubble_effect.cpp:371 spaces particles at exactly 2*pi*i/n and derives jitter only from the particle index, so every SparkBurst click renders an identical rosette. Needs a per-bubble deterministic seed (spawn coords + birth timestamp) driving angle scatter, speed/travel variance, particle size and per-particle alpha/lifetime, keeping the pure time-function contract (no per-frame state).
