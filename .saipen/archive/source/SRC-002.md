# User request

priority: P1
verify: hold produces visible continuous effect that ends on release; no leak when Up is lost; existing one-shot click behaviour unchanged when hold disabled; CTest green

## Request

Click HOLD effects: press-and-hold on any enabled mouse button must produce its own continuous, style-aware VFX distinct from the one-shot down bubble (charge/build-up while held, release payoff), driven by real ButtonAction::Up handling which the app currently ignores; per-style hold behaviour for the existing 11 ClickStyle families; config + GUI + persistence + tests.
