- [ ] T-34 [P2] Developer Release Defaults panel in dev builds only: Capture Current Settings, Save as Dev Preset, Apply Dev Preset, Promote Current or Selected Preset to Release Defaults, Apply Release Defaults to Current Settings and Show Difference From Release Defaults; named or timestamped complete AppConfig presets under dev/presets that never ship in the production package; promotion excludes runtime-only state, serializes deterministically, validates, writes atomically, re-reads and reports success only after roundtrip equality; promotion never modifies the live running configuration; a compact textual per-setting diff; ordinary production users never see developer authoring controls and a release build cannot write the repository defaults file | verify: the dev entry is absent from a production build and present in a dev build; capture produces the complete current semantic AppConfig; a named preset saves and re-applies to exactly the captured values; promote writes atomically and only reports success after re-reading and proving semantic equality, and never partially overwrites the canonical file; promote leaves the live configuration byte-identical; the diff view reports the expected per-setting differences; no dev preset or dev UI enters the production package; clean Release+Debug /W4 /WX and the existing suites green
