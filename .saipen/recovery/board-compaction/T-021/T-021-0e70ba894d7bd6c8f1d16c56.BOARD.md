- [x] T-021 Deterministic Trail Sparkles (HIGH effort; Phase 0 = trail enable-lifecycle freshness DONE + DRIFT RECOVERY: the in-tree sparkle implementation advanced past the E-115 lifecycle checkpoint before its dedicated verification was completed -- E-116/E-117 journaled PASS claims are NOT cited as proof; current cycle = own + finish + verify): Phase 0 = child Trail toggle clears the shared CursorHistory on BOTH transitions + one transparent clear frame on disable + Click effects untouched + deterministic regression coverage; then the sparkle overlay family on top of the accepted T-019 stroke: separate TrailSparkleMode enum (Off/Stardust/Twinkle/Glitter/Firefly, default Off; TrailStyle::Spark stays untouched), bounded TrailConfig sparkle fields (sparkle_amount 0..1 default 0.45, sparkle_size_px 1..8 default 3.0, sparkle_spread_px 0..32 default 10.0, named constants), schema 4->5 migration exactly once + roundtrip + invalid-enum recovery + numeric bounds tests, stateless deterministic sparkle model (NO frame-random particles: derive from path/sample identity, sample timestamp, arc-length slot, hash seed, now_ns; identical inputs emit identical geometry; frame cadence must not change sparkle identity), TrailGeometrySink add_sparkle decoration primitive (Dot/Cross/Diamond, Trail-owned, no D2D coupling), OverlayWindow cached-resource rendering (no per-sparkle brush/geometry allocation) with pre-draw culling and virtual-screen->overlay-local transform, four recognizably distinct mode characters, sparkle color derived from the local Trail color at the path position (Solid/Head/Tail/Gradient, no hard-coded white), hard emission cap 128 enforced before sink emission, Settings SPARKLES selector-button section under the T-020 selector contract with contextual parameter rows hidden when Off and bounded regions preserving anchors, Restore Trail Defaults + Restore All reset sparkles to Off+defaults while style/color changes never touch sparkle settings, determinism + mode-character + bounds test phases, 60s Classic+Glitter Amount 1.0 perf run, fresh portable artifact + user visual pass over 4 modes x 5 movement paths | verify: PHASE 0 DONE + ACCEPTED (E-115): lifecycle sound (clear both transitions, transparent clear frame, click independence, persistence); E-120 AUDIT CYCLE: (1) draw-order REPAIRED (deferred head flush before first sparkle, sequenced-sink regression proves all Trail including head before every sparkle); (2) path-occurrence timestamp identity REPAIRED (seed includes sample ts, figure-eight collision regression); (3) config/multimon/sparkle tests expanded; (4) GUI regression FOUND and test REPAIRED (click_style_options_region_reserves_space scroll-sensitive anchor measurement fixed to content-relative) -- repair NOT YET RERUN; (5) density DEFECT CONFIRMED (sparse=8/dense=21 2.625x, 4x threshold insufficient); ROOT CAUSE = one candidate per BuildPoint, clamp01(step/16) throttles dense but sparse long segments cannot emit multiple candidates; SPATIAL ARC-LENGTH SAMPLER INTRODUCED (E-122, UNVERIFIED): SparkleArcSampler generates candidates at fixed spatial intervals from same canonical geometry (polyline or Catmull-Rom subdivision) as visible Trail, residual preserved across pieces, stable slot identity (occurrence_ts + slot_ordinal + mode, never now_ns), multiple candidates per long segment; CONVERGENCE COMPLETE (E-124): identity split from bounded work counter (global k never hashed; seed = occurrence_ts + per-occurrence slot_ordinal), head-anchored slot lattice, synthetic tail and live head inherit REAL sample timestamps (no window_start/now_ns identity), animation age = (1-t)*lifetime_ms per slot, mode density applied exactly once (spacing = base/density, acceptance = Amount), whole-path candidate budget (spacing = max(normal, total_arc/128)) so the head is never starved, Twinkle 0.16->0.30 and Firefly 0.12->0.22 for full-strength visibility, pure test seam collect_sparkle_slots(); old 0.25..4.0 density gate DELETED, replaced by exact cross-sampling convergence (357 px at 3/7/17/51 px -> Stardust 10, Twinkle 5, Glitter 16, Firefly 4 in every representation), plus moving-tail identity, multi-slot age, curve-alignment (0.0000 px to the smoothed path vs 11.8765 px to the raw polyline), head-starvation and mode-visibility regressions; clean Release+Debug /W4 /WX 0 err 0 warn, CTest 15/15 both configs (input-injection suite excluded pending user consent -- ctest_safe.bat), sparkle 26/26, trail 12123 checks, gui 78/78 with the repaired layout assertion PASS unweakened, config 27/27, multimon 40/40, scheduler 12/12, lifecycle 6/6, 60 s soak bounded at the 128 cap with no accumulation, FRESH artifact ProTrail-T021-Test-x64 (exe SHA256 7C59D144..., zip 0718EBC1...) built after every source change and smoke PASS with zero orphans; full CTest INCLUDING protrail_input_dispatch CLEARED with user consent (Release 16/16, Debug 16/16); TWO PRODUCTION DEFECTS REPAIRED on user instruction (E-125): (1) the emitters published a raw linear base_opacity * t, so the Fade start slider and the Fade curve selector never reached the screen and the sparkle layer faded on a different curve than its own stroke -- both emitters now take alpha from alpha_for(); (2) the Dotted/Spark DotSampler anchored its arc-length lattice at the retracting TAIL, so every dot crawled backwards each frame and the running dot_index re-seeded the Spark flicker globally -- the lattice is now head-anchored off the measured canonical arc length with a separate bounded emit counter; plus TrailStyle::Pulse now breathes the sparkle layer too. New regressions cover all three. FINAL EVIDENCE: clean Release+Debug /W4 /WX 0 err 0 warn, CTest 16/16 both configs, sparkle 28/28, trail 12662 checks, stroke policy 267 checks, gui 78/78, config 27/27, multimon 40/40, scheduler 12/12, lifecycle 6/6, 60 s soak bounded at the 128 cap with no accumulation, fresh artifact ProTrail-T021-Test-x64 (exe SHA256 8D4FC1E1..., zip E916B820...) built after every repair, smoke PASS zero orphans. REMAINING: user visual PASS over 4 sparkle modes x 6 movement paths, plus confirmation of the two repairs (fade curve/fade start actually visible, Dotted/Spark dots no longer crawling); R1 VISUAL REPAIR CYCLE (E-126): PREVIOUS USER VISUAL RESULT = FAIL -- (1) the four sparkle families looked too similar; (2) sparkles moved WITH the Trail instead of staying at their spawn positions; (3) Comet/Pulse/Ribbon were insufficiently distinct from Classic and from each other. R1 REPAIR now in the accepted source: world-anchored source-occurrence sparkle lattice -- anchors are permanent and independent of the moving Trail head/tail/window; live/unfinalized spans cannot seed permanent sparkles (finalized geometry only); age-driven motion from stable spawn positions; per-family envelopes hardened (Stardust soft dust, Twinkle large Cross/Diamond flashes, Glitter dense scattering shimmer, Firefly sparse drifting luminous dots); Trail color/fade/Pulse inheritance and the hard 128 sparkle cap retained; Comet strong head-heavy width/alpha profile, Pulse traveling spatial width + alpha wave, Ribbon non-monotonic moving broad/narrow lobes. NOT retuned before user testing. R1 EVIDENCE (accepted, preceding the clean verification): Release CTest 16/16 PASS, Debug CTest 16/16 PASS, sparkle 30/30, GUI 78/78, clean Release+Debug 0 errors 0 warnings. DOC-ONLY FIX: Firefly density comment corrected to production truth 0.14 (kSparkleBaseSpacingPx 18 / 0.14 = ~129 px slot spacing), documented as INTENTIONALLY the sparsest family; the stale "0.22" note removed in trail_effect.cpp and test_trail_sparkles.cpp; NO retune. After that comment-only edit the tree was rebuilt (Release+Debug 0 errors 0 warnings) and re-verified: Release CTest 15/15 PASS on build_t021r1_clean with protrail_input_dispatch excluded (pointer-stealing SendInput suite, needs user consent); the accepted 16/16 gate stands for the semantically identical tree. FRESH R1 PACKAGE built from exactly build_t021r1_clean\Release\protrail.exe: artifacts\ProTrail-T021R1-Test-x64 (exe 397312 B SHA256 905C3254E3B68AD5738076A8C56D3AB38978098F786F7142C43E353276CFE50D) + artifacts\ProTrail-T021R1-Test-x64.zip (22644053 B SHA256 BCED523A5E04C54B64787A12F66702708A10383DB827B0977270E81AF451F012, 49 entries incl. protrail.exe, Qt6Core/Gui/Widgets, platforms\qwindows.dll, styles, imageformats, tls, translations); ordering proven in t021r1_package.txt: newest source/test mtime 2026-09-14T07:15:23.886Z < Release exe 07:15:41.733Z <= deployed exe / ZIP; the pre-R1 T021 hash 8D4FC1E1... is NOT reused. ISOLATED DEPLOYED SMOKE on the new R1 package (t021r1_smoke.txt): SMOKE_RUNNING_OK, SETTINGS_WINDOW_OPEN_OK, PROCESS_EXIT_CODE_0_OK, ZERO_ORPHANS_OK, SHUTDOWN_LOG_EVIDENCE_OK, CONFIG_PERSISTENCE_EVIDENCE_OK, NO_PRODUCTION_CONFIG_MUTATION_OK, NO_PRODUCTION_LOG_MUTATION_OK, SMOKE_NATURAL_SHUTDOWN_PASS, SMOKE_CLEAN_EXIT, no forced cleanup. SAMPLING SCOPE NOTE: world-anchored source-occurrence sampling intentionally permits MODEST particle-count differences across raw input rates (roughly Stardust 12-15, Twinkle 5-7, Glitter 22-25, Firefly 2-3 for equivalent path representations); exact input-rate independence is NOT claimed and the sampler is NOT rewritten now -- acceptable pending visual verification, reopen only as a calibration issue if the user reports visible density change with cursor speed. T-021 STAYS VERIFY: the ONLY authorized visual-test artifact is ProTrail-T021R1-Test-x64; blocker = user visual verification (sparkle spawn-anchoring test, sparkle family distinguishability test, Classic/Comet/Pulse/Ribbon distinctness test, plus Fade Start, Linear/Smooth/Ease Out, non-crawling Dotted/Spark, sparkles breathing with Pulse, no figure-eight teleporting, no cross-monitor clipping). NOT DONE. || MANUAL-VERIFY RESULT: PASS -- user visual verification of the T-021R1 artifact ProTrail-T021R1-Test-x64 (exe SHA256 905C3254E3B68AD5738076A8C56D3AB38978098F786F7142C43E353276CFE50D, zip SHA256 BCED523A5E04C54B64787A12F66702708A10383DB827B0977270E81AF451F012): "Da, prikolno, uzhe namnogo luchshee i zametnee". ACCEPTED VISUAL OUTCOME: (1) the sparkle modes are now meaningfully more distinguishable; (2) sparkle behavior is substantially improved and reads as an actual particle decoration rather than the previous moving-trail attachment -- world-anchored particle behavior ACCEPTED; (3) Comet / Pulse / Ribbon distinction ACCEPTED; the current Trail visual milestone is sufficient to proceed. NOT claimed: pixel-perfect finality -- minor future calibration remains allowed and is NOT a T-021 blocker. STANDING EVIDENCE AT CLOSE (E-125/E-126, not re-run for closure): clean Release+Debug 0 errors 0 warnings, Release CTest 16/16 PASS and Debug CTest 16/16 PASS, sparkle 30/30, GUI 78/78, world-anchored sparkle implementation with the bounded 128 sparkle cap, Comet perceptual profile, Pulse traveling wave, Ribbon non-monotonic lobes, Firefly production density 0.14 (documented, not retuned), deployed portable R1 package (dir + zip) with the ordering proof in t021r1_package.txt, isolated deployed smoke PASS with zero orphans and no production config/log mutation (t021r1_smoke.txt). NON-BLOCKING BACKLOG NOTES carried forward: Firefly density may receive later calibration if it proves too sparse; raw-input-rate sparkle-count variation is accepted unless it becomes visibly distracting; further artistic tuning must NOT reopen T-021 automatically.
