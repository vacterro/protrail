- [/] T-23 [P1] World-Anchored Trail Shards: additive TrailSparkleMode::Shards with Triangle geometry, deterministic world-anchored drift/rotation, UI/config/tests/package; stop at VERIFY pending user visual verification. | verify: BUILD COMPLETE, AWAITING USER VISUAL TEST. SCHEMA 7 ENUM-MIGRATION BOUNDARY (T-023 repair, done BEFORE Shards closure): kCurrentSchemaVersion 6 -> 7; ConfigStorage now gates newly introduced enum values on the SOURCE schema, not on the current enum, so a value that was invalid when the file was written can never be reinterpreted as a new visual effect -- sparkle_mode 5 means Shards only at source schema >= 7 (schema 5/6 files repair it to Off, the pre-T-023 contract), click style 7..10 mean Air/Fire/Water/Earth only at source schema >= 6 (schema <= 5 files repair them to Ring); named constants AppConfig::kShardsSparkleModeSchema / kElementalClickStyleSchema carry the boundaries; legitimate historical values load unchanged and any successfully loaded config saves at schema 7. CONFIG REGRESSIONS (2 new slots, config 32/32): schema 6 + sparkle 5 -> Off, schema 5 + sparkle 5 -> Off, schema 5 + sparkle 4 -> Firefly unchanged, schema 7 + sparkle 5 -> Shards, current-schema invalid sparkle -> Off, Shards save/load roundtrip at schema 7, migrated old file re-saves at schema 7 and still loads Off; schema 5 + click 7/8/9/10 -> Ring, schema 5 + click 6 -> DotRing unchanged, schema 6 + click 7..10 -> Air/Fire/Water/Earth, current-schema invalid click -> Ring. SHARDS IMPLEMENTATION: Shards drift folded into the sparkle-mode switch in trail_effect.cpp (no trailing special case, no unhandled enumerator under /W4); Triangle-only geometry; identity = occurrence_ts + slot_ordinal + mode; movement derived only from the immutable world anchor and age, so advancing the head never drags a live shard; deterministic CW and CCW rotation; deterministic size variation; colour from the local Trail colour; cached ID2D1PathGeometry reused per shard via transform, recreated on device loss, zero per-shard allocation. SPARKLE TEST COVERAGE EXTENDED (sparkle 33 passed + 1 opt-in soak skip): Shards added to the trail-segments-unchanged sweep, the high-speed cap sweep, the finite/negative-coordinate envelope sweep, the cross-sampling input-rate convergence sweep and the local-Trail-colour sweep; new deterministic density minimum shards >= 8 with twinkle < shards < glitter (measured stardust=15 twinkle=5 glitter=24 firefly=3 shards=10); the four existing Shards regressions (Triangle-only, determinism + surviving world anchors, drift/rotation/size with age, bounds + Amount 0 + cap) retained unweakened. GUI 83/83 (1 new): sparkle_mode_switching_preserves_anchors_and_region sweeps all six modes and proves the STYLE/COLOR/SHAPE/TRAIL/SPARKLES anchors do not move and the reserved Amount/Size/Spread region keeps one constant height, with the Shards rows shown inside it. AUTOMATED EVIDENCE on build_t023_clean: clean Release+Debug /W4 /WX 0 errors 0 warnings; CTest Release 15/15 PASS and Debug 15/15 PASS with protrail_input_dispatch EXCLUDED -- that suite injects real SendInput mouse moves/clicks and steals the pointer, so the full 16/16 run is NOT claimed and needs explicit user consent; trail 12667 checks 0 failures; stroke policy 267 checks 0 failures; 60 s Glitter soak PASS (frames=3750, max 128/frame, first6s_max=128 last6s_max=128, no accumulation; the soak suite is Glitter-only by design -- Shards is sparser and bounded by the same cap). ARTIFACT artifacts\ProTrail-T023-Test-x64 exe 404992 B SHA256 CB498A5150D192EA7EBB0B9181E429BC82F7C316536D4E28E03B8FDD4C96F2E8 + zip 22648273 B SHA256 090A2981D10016C22039EC2063FAEF4DDF422A05D2D82F4C6439157453D45ED5; ordering proven in t023_package.txt (newest source 2026-09-14T14:52:22.830Z < Release exe 14:58:45.476Z <= deployed exe / ZIP); the T-022 and T-021R1 binaries are NOT repackaged. NOTE: the first package attempt was refused by its own ordering check because the build was incremental over a failed first configure; build_t023_clean and the artifact were deleted and rebuilt from scratch, and the evidence above is from that genuinely clean tree. ISOLATED DEPLOYED SMOKE PASS (t023_smoke.txt): SMOKE_RUNNING_OK, SETTINGS_WINDOW_OPEN_OK, PROCESS_EXIT_CODE_0_OK, ZERO_ORPHANS_OK, SHUTDOWN_LOG_EVIDENCE_OK, CONFIG_PERSISTENCE_EVIDENCE_OK, NO_PRODUCTION_CONFIG_MUTATION_OK, NO_PRODUCTION_LOG_MUTATION_OK, SMOKE_NATURAL_SHUTDOWN_PASS, SMOKE_CLEAN_EXIT. REMAINING: user visual test of Shards on the artifact above (slow straight motion, fast straight motion, circles/curves, direction reversal, brief stop/resume; confirm fragments read as detached world-anchored debris, not glued to or dragged by the cursor), plus the consented full-CTest run including protrail_input_dispatch. KNOWN NON-BLOCKING: stale density comments in test_trail_sparkles.cpp full_strength_modes_meet_deterministic_minimums say 0.55/0.85 for Stardust/Glitter where production is 0.72/1.25 -- assertions and behaviour are correct, comments only, not repaired in this ticket. | owner: antigravity | claim_time: 2026-09-14T15:03:50Z
