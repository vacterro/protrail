- [ ] T-50 [P1] REGRESSION against T-037 intent: src/ui/main_window implemented the Main Essentials surface and Application constructed a canonical MainWindow, but product navigation never made it user-reachable -- show_settings() only showed SettingsWindow, the manual startup path showed SettingsWindow directly, the tray Settings action routed to show_settings(), and no product-home tray entry existed, so T-037 produced an implementation without a user-visible Main home surface. REPAIR: a manual launch opens Main and does not open the advanced editor; an autostart launch stays tray-only; the tray carries an explicit product-home action plus Settings; Main carries Advanced Settings which restores the existing SettingsWindow; closing Main hides to tray; exactly one canonical MainWindow and one canonical SettingsWindow exist under repeated tray actions, double-clicks, second-instance activation and Main to Advanced Settings. T-037 historical closure untouched. | verify: protrail_tray_tests proves manual startup shows Main and not Settings, autostart shows neither surface, the tray home action and Advanced Settings restore the SAME instance, second-instance activation restores Main exactly once, Main and Settings stay synchronized in both directions, and Restore Defaults lands on both surfaces as one silent transaction with one persistence commit; protrail_main_window_tests proves widget identity, silent population, one publication per action, the exact semantic field per control, contextual enable states and hide-to-tray close; Release and Debug are /W4 /WX clean with 23/23 CTest green in both configurations
