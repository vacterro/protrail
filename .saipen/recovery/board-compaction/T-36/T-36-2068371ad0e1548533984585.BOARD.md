- [ ] T-36 [P1] Advanced Motion Wake controls and movement-condition state: Wake Strength, Wake Size, Wake Spread, Speed Response, Minimum Motion Speed, Turn Accent and Stop Accent as bounded artistic multipliers and conditions that never expose internal safety caps and never directly multiply particle count; a small deterministic Stationary/Moving/Turning/Stopping state layer driven by input timestamps and movement samples with hysteresis, immune to frame cadence, tiny jitter, lost-Up cancellation and event-frequency spam; turn and stop accents are bounded, style-aware and world-anchored after birth; Motion Wake OFF disables detached wake and motion accents, Hold FX OFF clears all Hold/Wake state; a schema bump migrates existing accepted configurations to exactly the neutral identity values that reproduce the current accepted appearance | verify: strength, size, spread and speed-response bounds hold and out-of-range or NaN input repairs to the documented default; the minimum-speed gate suppresses detached wake below the configured threshold and restores normal wake above it without touching the Hold aura; a turn accent fires only on a meaningful directional transition and never on tiny jitter; a stop accent fires at most once per movement-to-stationary transition and movement restarts re-arm it; accents stay world-anchored after birth; an upgraded pre-existing config reproduces the accepted appearance exactly; existing wake lifetime and scheduler contracts stay green; hard caps remain respected; no NaN or Inf reaches geometry; equal inputs and timestamps stay deterministic; clean Release+Debug /W4 /WX with 0 warnings 0 errors and the existing suites green
