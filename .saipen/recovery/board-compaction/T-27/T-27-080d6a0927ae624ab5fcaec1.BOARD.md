- [ ] T-27 [P1] Hold FX Controls: retire the T-24 one-toggle limitation and expose real artistic Hold controls without building a particle-editor zoo. Add a compact HOLD section under Click with Hold FX, Motion Wake, Hold Intensity, Wake Density, Wake Life and Release Strength. ClickStyle stays the only style selector and the activation threshold stays hidden so the short-click / HOLD gesture boundary remains stable. Wake Density maps to spatial emission SPACING, never particles per frame, through a smooth bounded curve (about 32 px at 0.25, 12 px at 1.00, 6 px at 2.00) while the style particle_amount keeps owning the within-emission particle budget. Wake Life is SNAPSHOTTED at birth so already-born emissions never retroactively stretch or shrink. Hold Intensity primarily scales the attached aura without multiplying particle count. Release Strength transforms the existing charge model, with 1.0 reproducing the accepted T-24 release and particle count still bounded by existing budgets. Introduce schema 8 -> 9: historical configs keep accepted HOLD behaviour exactly, default Motion Wake OFF because it is materially new, and reset all four multipliers to baseline; fresh schema-9 installs default Hold FX and Motion Wake ON. Restore Click Defaults must restore Hold too; Wake controls disable logically when Motion Wake is OFF; layout stays stable with no surprise vertical resizing. | verify: config roundtrip for all six Hold fields with hard bounds honored; schema-8 migration preserves hold_enabled exactly, defaults Motion Wake OFF and resets Hold Intensity, Wake Density, Wake Release Strength to 1.0 and Wake Life to the chosen baseline; fresh schema-9 defaults Hold FX and Motion Wake ON with every multiplier at baseline; programmatic population stays silent and each user action publishes exactly one coherent ClickConfig; Restore Click Defaults restores Hold settings; Wake-specific controls disable when Motion Wake is OFF and all Hold controls disable when Hold FX is OFF; minimum / default / maximum of each setting produces no NaN, no runaway particle count, no unbounded geometry, no unrecoverable invisible state and no stale wake after disabling Hold FX; 1.0 reproduces the accepted T-24 baseline appearance; clean Release and Debug /W4 /WX; CTest both configs; fresh combined portable artifact and isolated deployed smoke.
