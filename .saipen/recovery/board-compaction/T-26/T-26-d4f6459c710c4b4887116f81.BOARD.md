- [ ] T-26 [P1] Hold Motion Wake / world-space style emissions: while an ActiveHold moves, continuously shed style-specific emissions into WORLD SPACE. Each emission gets an immutable birth anchor, birth timestamp, deterministic seed and a full style/colour/charge/tangent snapshot; after birth it NEVER follows the cursor and its geometry is a pure function of elapsed time. Emission is DISTANCE-based, not per mouse event and not per frame: walk each previous->new movement segment at a configurable spacing, interpolate both anchor and birth timestamp along the segment, and bound births per movement sample (kMaxWakeBirthsPerMovement) plus a global cap (kMaxWakeEmissions, prune expired first then discard oldest) so a cursor teleport cannot storm allocations. Motion energy is a bounded clamp(speed / reference) artistic input. All 11 ClickStyles get a distinct wake grammar: ring echo stamps, paired concentric resonances, phased world-space ripple sources, reduced micro-burst packets, crackling spark shedding, soft afterglow puffs, detached bright beads, tangential motes and eddies, rising embers, droplet plus splash arcs, heavy dust and settling debris. The attached aura keeps following the cursor and release payoff is unchanged; stationary holds emit no persistent wake; Up and lost-Up stop new births while already-born wake finishes its own fade; master OFF, Click FX OFF, Hold FX OFF, Motion Wake OFF and explicit clear all wipe wake; the active-only scheduler stays awake while any wake is alive. | verify: same geometric path delivered as one large movement event versus many small collinear events produces equivalent wake anchor positions and counts; no wake before the Candidate->ActiveHold activation threshold; a stationary ActiveHold creates zero detached wake births; each anchor stays fixed at its birth coordinate when the cursor later moves; Up stops new births while born wake keeps fading until expiry; lost-Up cancels births without a release payoff; master OFF / Click FX OFF / Hold FX OFF / Motion Wake OFF each clear alive wake; global cap and per-movement birth cap both respected; no NaN/Inf; multi-monitor virtual coordinates remain valid; all 11 styles proven structurally (Ring emits ring geometry and no generic particle cloud, DoubleRing paired rings, Ripple phased geometry, Burst a reduced radial packet, SparkBurst stronger randomized per-particle variation, SoftFlash fill-dominant glow, DotRing bead identity, Air tangential/angular displacement, Fire particles rise over lifetime, Water droplets plus ripple component, Earth heavy low settling rather than orbiting); changing style or colour affects new wake only; equal emission record plus equal elapsed time renders identical geometry at different frame cadences; clean Release and Debug /W4 /WX; CTest both configs; fresh portable artifact and isolated deployed smoke.
