- [ ] T-37 [P1] Main UI Essentials quick controls: a compact two-column Essentials surface on the Main window exposing Master FX, Trail FX, Trail Style, Sparkle Mode, Click FX, Click Style, Hold FX, Motion Wake, Wake Density, Hold Intensity and Start with Windows without mirroring the whole Settings window; Main and Settings edit the same AppConfig and stay synchronized in both directions through one coherent persisted update with silent programmatic population and no callback recursion or duplicate save storm; advanced controls stay in Settings; startup behaviour is predictable (manual launch preserves current behaviour, autostart is tray-only without focus steal, tray Open/Settings activates intentionally, closing Main keeps existing tray semantics and Start with Windows never reopens Main) | verify: programmatic population emits nothing; each Essentials control updates exactly one intended field; Main to Settings and Settings to Main synchronization both hold; no recursive callbacks and exactly one persisted update per user change; Restore Defaults updates the Main surface correctly; autostart launch stays tray-only with no focus steal while a manual launch preserves the current user-facing behaviour; clean Release+Debug /W4 /WX with 0 warnings 0 errors and the existing GUI suites green
