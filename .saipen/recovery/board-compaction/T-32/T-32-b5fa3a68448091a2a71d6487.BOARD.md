- [ ] T-32 [P1] Silent Windows autostart: per-user HKCU Run entry under a stable ProTrail-owned value name whose command is the fully quoted current executable path plus an explicit startup argument; a startup launch initializes tray, effects and render infrastructure but never shows or activates the Main/Settings window, never steals focus, never flashes a normal window and creates no taskbar button; no elevation; autostart respects the existing single-instance contract and never opens the Main UI merely because the second invocation came from autostart; the registered command is reconciled to the real current executable path after the portable app moves and disable removes only the ProTrail-owned value; registry access is abstracted behind a small testable platform component; a user-facing Start with Windows setting exists and pre-autostart configurations migrate to OFF | verify: enabling writes the expected quoted command containing the real executable path and the startup argument; disabling removes only the owned value and leaves unrelated startup entries byte-identical; a path containing spaces is quoted correctly; a moved executable reconciles the owned command; repeated enable and repeated disable are idempotent; the startup-mode command line selects the silent path and does not request the Main/Settings window; the single-instance contract is honoured; registry failures surface as a reported non-success; every unit test drives an in-memory backend and the real Run key is proven untouched by a before/after read; clean Release+Debug /W4 /WX with 0 warnings 0 errors and the existing suites green
