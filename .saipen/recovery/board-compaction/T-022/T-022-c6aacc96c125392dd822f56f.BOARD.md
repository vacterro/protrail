- [x] T-022 Elemental Click VFX (HIGH effort): four new elemental click styles on top of the accepted T-017 click-effect architecture -- Air / Fire / Water / Earth, added as ClickStyle values 7..10 (the seven existing styles Ring/DoubleRing/Ripple/Burst/SparkBurst/SoftFlash/DotRing stay byte-identical). USER DECISION (this run): elemental identity = MOTION/SHAPE **plus** an elemental HUE BIAS blended over the user's Click color (not fixed colors, not motion-only) -- new bounded ClickConfig field element_tint 0..1 default 0.65, schema 5 -> 6 migration exactly once (a schema-5 file simply lacks the key and keeps the struct default), invalid style enum recovers to Ring, element_tint clamped. MOTION SIGNATURES (pure functions of progress + particle index, stateless like the rest of the effect, no new sink primitives -- rings via add_bubble, dots via add_particle): Air = fast thin wide ring, no fill, particles drifting TANGENTIALLY (angle advances with progress) while creeping outward, small and faint; Fire = no ring, small soft base flash, particles RISE (y decreases) with lateral wobble, shrinking, deterministic flicker, tint cooling with age; Water = up to 3 CONCENTRIC ripple rings born at staggered progress phases, thin, no fill, outer-first ordering; Earth = thick low dust ring reaching only ~60% radius plus few CHUNKY debris particles thrown outward under GRAVITY so they arc and end BELOW the anchor. Bounded: particles per click <= particle_amount (0..24), rings <= 3, kMaxActiveBubbles 128 unchanged. Settings: four more selector buttons in the click STYLE section under the T-020 selector contract + an Element tint row shown only for the four elemental styles, anchors preserved. Tests: per-element motion-signature regressions (Air angular advance, Fire monotone rise, Water ring staggering/ordering, Earth gravity arc + larger debris), determinism, particle bounds, tint blend endpoints (0 = exact user color, 1 = exact element target) and per-element distinctness, schema 5->6 roundtrip + invalid-enum recovery, GUI selector/visibility coverage. Then clean Release+Debug, full CTest, fresh portable package, isolated deployed smoke, user visual gate. NON-GOALS: no shaders, no particle editor, no Trail/Sparkle changes, no retune of the seven existing click styles | verify: BUILD COMPLETE, AWAITING USER VISUAL TEST. Clean Release+Debug /W4 /WX 0 errors 0 warnings (build_t022_clean); CTest Release 15/15 PASS and Debug 15/15 PASS with protrail_input_dispatch EXCLUDED (that suite injects real SendInput mouse moves/clicks and steals the pointer -- the full 16/16 run is the remaining gate and needs explicit user consent); click effect suite 1822 checks 0 failures; config 30/30 (3 new: schema5->6 element_tint default, all 11 click styles roundtrip with element_tint, invalid style/tint recovery); GUI 82/82 (4 new: elemental selector exclusivity + publication, Element tint row visible only for the four elements and Fire hiding Outline, tint slider bounds + exactly-one publication, Restore Click Defaults resets to Ring + 0.65); the anchor-stability sweep click_style_options_region_reserves_space now covers ALL ELEVEN styles. LAYOUT DEFECT FOUND AND REPAIRED DURING BUILD: adding the second options row pushed the Click page past the scroll viewport, after which hiding the Outline row (Spark Burst, Soft Flash, Fire) re-flowed the ANIMATION section and moved its anchor by 2 px -- Outline and Fill opacity now live in a bounded fixed-height host (click_param_options), exactly like click_style_options, so section heights are constant for every style. ARTIFACT artifacts\ProTrail-T022-Test-x64 exe 402944 B SHA256 8637B145848EF26417AA5B8E31A8D02955AE4446A7421A40D053FC7E3BA445DB + zip 22647191 B SHA256 4199C3E545C1646CA6C084844D9D6067006F6469671D6D77CA4E5560BFB68B63, ordering proven in t022_package.txt, the T-021R1 binary is NOT repackaged. Isolated deployed smoke PASS (t022_smoke.txt): running, Settings window, exit 0, zero orphans, isolated config+log, no production config/log mutation. REMAINING: user visual test of Air/Fire/Water/Earth and the Element tint slider, plus the consented full-CTest run | owner: antigravity | claim_time: 2026-09-14T09:09:14Z | closure_mode: own_patch
