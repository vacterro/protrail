- [ ] T-33 [P1] Canonical Release Defaults system: one authoritative resources/release_defaults.json holding every persisted user-facing setting (Trail, Trail Style, sparkles, Click, ClickStyle, Hold, Motion Wake, application behaviour, Start with Windows) and no machine-specific runtime values; validated at build time and packaged or embedded so the shipped executable never reads a writable source-tree JSON; a missing or invalid source fails the release build visibly instead of silently shipping unknown fallback values; this single semantic source drives BOTH fresh configuration creation and Restore Defaults; pre-feature configurations migrate without surprise and no release-default change retroactively overwrites an existing saved preference | verify: the canonical defaults file loads and validates; a fresh configuration equals the Release Defaults; Restore Defaults reproduces the Release Defaults; capture then serialize then parse roundtrips to equality; the source file is replaced atomically; a missing or invalid defaults source fails the build/test gate visibly; the production package embeds the validated defaults and contains no writable source-tree defaults path; every persisted setting is covered; clean Release+Debug /W4 /WX with 0 warnings 0 errors and the existing config suites green
