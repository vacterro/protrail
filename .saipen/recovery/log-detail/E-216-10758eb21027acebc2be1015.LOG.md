- 16.09.26 16:14 [E-216] [parent: E-215] [T-26] [agent: antigravity] [op: ticket-09f02f21a5ed4f0b9a3923951e2c8b80] DEC: ticket unblock via SAIOPS -- REPAIR REQUIRED before visual gate (handoff SRC-005, work T-28). Discovered correctness defects: (1) has_live_wake() inferred liveness from birth order (checked only wake_.back()), so after a Wake Life change a newer short-lived emission could expire while an older long-lived one stayed alive -- the active-only scheduler could false-idle and freeze live wake. (2) on_cursor_moved() gated an entire movement segment on the END timestamp via is_active_at(), so a segment straddling the Candidate->ActiveHold activation emitted wake with birth_timestamp_ns before activation, violating candidate-never-emits. (3) zero-particle Burst/SparkBurst wake_marks() computed sector = two_pi/count with count 0, a non-finite intermediate in a finite-math path. Repair A/B/D lands under T-28; ticket returns to WAIT_USER_CONFIRMATION only after fresh Release+Debug verification and a fresh combined artifact.
